# 素打目标手机与打印机验收清单

本清单用于验证无法在无设备构建机上替代的相机、蓝牙和出纸结果。每项都应记录手机型号、Android 版本、打印机型号、纸张、应用版本和失败日志。

## 1. 安装与基础稳定性

- 安装唯一正式包，确认桌面名称为“素打”、图标有完整背景。
- 冷启动 10 次；依次进入文字、图片、二维码、一维码、便签、自由画布、模板、历史、设置，不闪退。
- 故意触发一次可恢复错误，确认错误信息可以复制、分享并保存为 `.md`。

## 2. PP-OCRv6 Small 真实准确率验收

- 建议至少准备 100 张真实样本、总计不少于 10,000 个标准答案字符，覆盖中文/英文/数字、小字、倾斜、反光、低照度、密集标签。
- 对每张图拖动四边/四角调整识别区，点按文字完成对焦后拍照扫描，再在校对页逐字修正并勾选“对照原图逐字校对”。
- 设置页中的累计准确率使用编辑距离计算：`1 - 字符错误数 / 标准答案字符数`。模型置信度不是准确率承诺；底线 97%、目标 99% 必须用这批真实样本实测确认。
- 不能只挑识别成功的图片。拒绝输出、超时和无法识别必须单独计入样本结果。
- 连续扫描建模板 30 次，确认文字框位置、换行和可编辑内容；不要只看截图相似度。

## 3. 实时识码

- 对实际会用的每种二维码/一维码至少准备 10 个不同内容，在正常光、弱光、倾斜三种条件各扫一次。
- 核对返回内容逐字符完全一致；非法内容应明确提示或可靠回退，不能生成空白图。
- 验证可拖动识别区四边/四角、移动选区、点按对焦、补光灯和连续识别稳定性。

## 4. 纸张与打印校准

- 连续纸至少测试 10、20、30、40、50、54.9、55、57 mm；小于 55 mm 分别测试左靠和右靠。
- 标签纸至少测试 30×20、40×30、50×30、50×50、57×30 mm，并补测实际自定义宽高。
- 先打印内置校准页，测量左右边界和 10/20/30 mm 标尺；在设置中记录打印机偏移补偿。
- 每种宽度打印包含左边线、右边线、二维码、小字和图片的测试页，确认没有截断、越过纸边或额外打印空白区。
- 连续纸验证内容改变后自动长度随内容变化；标签纸验证输出长度始终等于设置高度。
- 升级后确认原旧默认 8 mm 自动变为 5 mm；再手动设置一个非 8 mm 数值并重启，确认用户值不会被迁移覆盖。
- 标签纸分别将额外走纸/标签间隙设为 0、5、10 mm 各打印一次，用尺实测相邻标签的走纸差值，确认 Qring 固件实际执行。
- 设备校准中的横向和纵向偏移分别测试 `-2.0 / 0 / +2.0 mm`；每次重新进入新建、模板、历史和导入画布打印校准十字，确认水平/垂直各自移动约 2 mm。
- 对以下六个入口分别打印，不能只用最终预览代替出纸：
  - 文字排版：中英文、数字、两种字体、400/700 字重和至少三行自动换行。
  - 图片打印：照片与黑白 Logo 各一张，至少比较阈值和 Floyd–Steinberg 两种算法。
  - 二维码：打印后用另一台设备回扫，内容必须逐字符一致，四周静区完整。
  - 一维条码：Code 128 与实际使用的商品码各一张，回扫内容必须一致且条高完整。
  - 便签纸：横线/方格/清单任选两种，确认末行和尾部走纸都能露出撕纸口。
  - 自由画布：保留默认文字打印一次，再删除默认层并加入图形/图片打印一次；空画布应提示而不是假装成功。
- 上述六个入口至少各测一次连续纸和一次标签纸；每次记录最终预览截图、出纸照片、纸宽/高度、装纸方向和是否回扫成功。

## 5. 蓝牙与状态

- 点“扫描设备”后应立即出现扫描动画；首次授权后授权卡片应立即消失并自动继续扫描。另从系统“应用权限”页授权后返回一次，卡片也应消失。扫描应允许手动停止；若 20 秒无结果应退出扫描态并给出提示，不能永久显示“扫描中”。
- 同时存在多个设备时，名称含 Qring 的设备应排在最前；核对已配对设备不会因尚未收到发现广播而消失。
- 首次配对、断开、关机重开、App 重启、手机蓝牙重启各测试一次自动重连。
- 验证电量和纸张状态只在设备支持查询时显示真实值，不支持或返回 `0xFF` 时显示 `--`，不能伪造 100%。分别在低电、充电中和充满后记录灯效、App 读数和十分钟后的再读数。
- 缺纸、恢复装纸和传输中断分别测试；批量任务应停止或按选择跳过，并能从准确记录/份数继续。
- 上盖合好后连续打印 20 份、停顿后再打印 50 份，确认不会在第几张后偶发误报开盖。然后在一份打印中真实打开上盖，确认 250 ms 确认窗口后仍能正确中断并提示。
- 在标签纸模式先后打印 2、5、6、10、20、50 份，连续纸模式再测试 5、6、10 份；必须核对实际张数与应用显示的已完成份数一致。逐张加入清晰序号，确认第 2、4、6 张不再空白、被固件吞掉或隔张打印。每份应是一次完整协议会话；上一份收到 ACK 后，必须再经过至少 6.5 秒固件复位等待（若点阵高度需要更久则按更久者），才允许打开下一份会话。
- 导出运行日志并按份核对“逐份物理完成闸”和“打印份数物理确认”：每份日志应明确包含“ACK 后固定复位 6500 ms”，且第 `N+1` 份的会话不得早于第 `N` 份的物理确认。批量期间不应再出现“打印份间状态位不可用”或仅由单次状态快照产生的“打印头过热”。若实际纸张少于应用确认数，保留完整 `.md` 日志、每张序号照片以及纸型/尺寸，不能只依据进度条判定。
- 在首页“走纸”页面分别点选 1/2/3/4/5 mm，再手输 10.5 mm；每次用尺测量累计走纸距离，并验证未连接时不会伪报成功。

## 6. 模板、字体和批量数据

- 每个行业随机打开至少 10 个模板，确认画布按真实比例显示，文字/编码/线条可独立编辑，且模板不会挤成一团。
- 导入 TTF 和 OTF 各一个，预览与打印比对同一字形；删除字体文件后的文档应安全回退并给出提示。
- 使用 Excel、XLS、WPS 另存 XLSX、CSV 各做一次多工作表测试；验证日期、百分比、筛选、排序、范围、份数、停止和续打。
- 制造一条非法条码和一次蓝牙失败，分别验证“停在失败位置”和“记录失败后跳过”两种策略。

## 7. 主题与系统入口

- 切换全部 10 套主题（含 4 套玻璃布局），检查首页、模板、编辑器、设置和弹窗均可读且控件不截断。
- 从相册、WPS/文件管理器分别分享图片、PDF、TXT/Markdown、CSV/XLS/XLSX、素打 JSON 模板到 App，确认进入正确本地流程。

完成以上清单并保留实际日志、照片和校对统计后，才能把对应设备组合标记为“实机通过”。
