# 素打 2.5.6 功能验证报告

验证日期：2026-08-20  
应用 ID：`com.qrint.studio`  
显示名称：素打  
版本：`2.5.6 (26)`
兼容清单：`minSdk 24 / targetSdk 36`，即 Android 7.0–16。

## 本次结论

文字识别已由旧模型完整替换为官方、未修改的 **PP-OCRv6 Small** 检测与识别模型，不存在 v5 推理备用路径或 v5 生产资产。相册 OCR、拍照扫描文字与拍照扫描建模板继续复用同一进程级 PP-OCR 会话。Release 依赖树和 APK 条目均未发现 ML Kit、OpenCV 或 RapidOCR。

官方检测模型使用 ONNX IR v10；为保留原始模型且使用 ONNX Runtime Android 1.21.1，最低系统提高为 API 24。Android 5/6 不再支持安装。

当前源码已通过本机构建机可执行的自动验证和正式发布构建。文字排版、图片、二维码、一维条码、便签和自由画布均通过相同的“语义预检 → 203 dpi 二值点阵 → 非空点阵校验 → 协议发送 → 尾部走纸”链路；两种纸型与 Qring/通用 ESC/POS 两种协议的组合均纳入单元测试。构建机没有连接 Android 真机或热敏打印机，因此摄像头、蓝牙和真实出纸仍必须按 `DEVICE_ACCEPTANCE_CHECKLIST.md` 做目标设备验收。

2.5.1 修复了 Android 12–16 蓝牙授权刷新、画布最小缩放闪退，并将连续纸默认尾部走纸统一为 5 mm。2.5.2 进一步修复标签纸额外走纸、设备校准与电量 `0xFF` 误报 100%。2.5.3 修复批量打印时 ACK 尾字节被下一份误判为开盖。2.5.4 加入设备空闲确认与首页独立毫米走纸。2.5.5 建立逐份 ACK、机械等待和断点恢复状态机。2.5.6 根据用户提供的 Qring_50EE 真实日志修正了关键假设：该固件可在打印引擎尚未复位时返回 `0xAA`，约 1.5 秒后开启的下一会话会被静默吞掉，而经过约 6 秒等待的会话均能出纸。因此多份打印现统一为“清除残留回包 → `ENABLE` → `ENABLE_SECONDARY` → `WAKE_UP` → 完整光栅 → STOP → 当前份 ACK → 点阵高度截止时间与 ACK 后固定 6.5 秒复位时间取较晚者 → 下一份”。批量期间不再轮询会被标签间隙和电机切换干扰的纸张/忙闲/温度状态；只有打印回包中的明确 `FF 01..04` 故障帧有效，且只有 `FF 03` 进入过热恢复。完成该闸后才递增已完成份数。另修复 Android 16 在相机权限被撤销后仍直接启动 `TakePicture` 导致的 `Permission Denial`：现在先请求权限，授权后自动继续原拍照动作，拒绝则安全清理临时 URI。软件状态机与权限分支已通过自动验证，但目标打印机实际张数仍列为实机验收项。

## 自动验证结果

| 检查 | 结果 | 证据 |
|---|---:|---|
| Debug 单元测试 | 163/163 通过 | App 153 + PP-OCR 10 |
| Release 单元测试 | 163/163 通过 | App 153 + PP-OCR 10 |
| Release Lint | 0 Error | 58 条非阻断建议；无阻断发布的问题 |
| Debug/Release 验证与 Release 构建 | 通过 | 分段执行 `testDebugUnitTest testReleaseUnitTest`、`lintRelease`、`assembleRelease` |
| Android 测试包编译 | 通过 | `:app:assembleDebugAndroidTest`；无连接设备，未冒充已运行 |
| 官方 ONNX 图冒烟 | 通过 | ORT Desktop 1.20.1：检测 `1×1×736×736`，识别 `1×40×18710` |
| Android OCR 运行库 | 通过依赖审计 | `onnxruntime-android:1.21.1`，无 OpenCV/RapidOCR/ML Kit |
| 字典/模型匹配 | 通过 | YAML 18,708 项 + 普通空格 + CTC blank = 18,710 类 |
| 模型完整性 | 通过 | 源码测试及 APK 内重新计算的长度、SHA-256 均与固定官方值一致 |
| APK 签名 | V2/V3 通过 | Android `apksigner verify --verbose --print-certs` |
| APK 清单 | 素打、2.5.6、minSdk 24、targetSdk 36 | Android `aapt dump badging` |
| APK ZIP 对齐 | 16 KB 校验通过 | Android `zipalign -c -P 16 -v 4` |
| OCR 旧资产审计 | 0 项 | APK 内无 v5、OpenCV、RapidOCR、ML Kit 文件 |

唯一正式 APK：`app/build/outputs/apk/release/app-release.apk`  
文件大小：128,747,184 字节  
SHA-256：`A4DA67135D0B40BC2491C5D838E927ABFD2032BBEA0C4ABB68A654A5D8DD11C6`  
签名证书 SHA-256：`AA1C259431F984149A9771171639B80F94D3F8A70283AACDF937F689FAE0131F`

## PP-OCRv6 Small 替换范围

- 内置官方 PP-OCRv6 Small 检测模型、识别模型和字符配置，首次使用无需联网下载。
- 检测模型：9,880,512 字节，SHA-256 `d73e0058b7a8086bbd57f3d10b8bcd4ff95363f67e06e2762b5e814fe9c9410e`。
- 识别模型：21,159,378 字节，SHA-256 `5435fd747c9e0efe15a96d0b378d5bd157e9492ed8fd80edf08f30d02fa24634`。
- 识别配置：150,579 字节，SHA-256 `ab078671bb49f06228eadccd34f1bb501e157f7a047095ffb943ba81512c77d1`。
- 检测和识别统一按模型配置使用 BGR 通道；检测阈值、框阈值和展开比采用官方 v6 Small 配置。
- 启动时验证资产长度、SHA-256、输入/输出张量形状和 18,710 类 CTC 契约；损坏或错配会显示明确错误，而不是静默输出错误文字。
- 预处理、DB 后处理、透视裁切、阅读顺序与 CTC 解码由 Kotlin/Android Bitmap 实现，不依赖 OpenCV/RapidOCR。
- 推理会话全局复用并串行执行，识别逐行运行且限制最大宽度/行数；低内存回调会释放原生会话，下次按需重建。
- 升级首次启动会删除应用私有目录中的旧 v5 模型缓存；源码生产资产和最终 APK 只保留 v6 Small。

## 已有自动测试覆盖

纸张与校准、连续纸算长、标签固定宽高、10–57 mm（每 0.1 mm）窄纸窗口、左右装纸、203 dpi 点数换算、画布边界/磁吸、文字/日期/流水号随选框缩放、数值输入草稿、模板质量、条码标准化、变量数据导入、批量打印游标、Qring 协议、每份 ACK 后固定复位截止时间、手动走纸状态防抖、蓝牙设备排序/自动重连退避、抖动、声音、崩溃 Markdown 报告，以及 PP-OCR 模型哈希、字典契约、BGR 预处理、阈值、CTC 解码、图像尺寸与 DB 后处理。

## 六类打印入口验证

| 入口 | 软件链路结果 | 关键检查 |
|---|---:|---|
| 文字排版 | 通过 | 默认文字非空；连续纸按 `StaticLayout` 的真实打印字体与宽度自动增高 |
| 图片打印 | 通过（选择有效图片后） | 未选择/无法解码/抖动后全白会在传输前阻止并显示原因 |
| 二维码 | 通过 | 默认 QR 内容合法；与一维条码分入口并经过条码标准化和生成冒烟测试 |
| 一维条码 | 通过 | 默认 Code 128 内容合法；非法格式不会发送空白点阵 |
| 便签纸 | 通过 | 横线、方格、清单、空白便签均生成可编辑元素；统一尾部走纸策略 |
| 自由画布 | 通过 | 初始提供可删除的普通文字层；空画布或全白点阵明确拦截，不再静默“发送成功但不走纸” |

以上“通过”表示模型、点阵、协议分支和走纸命令的自动化软件链路通过，不表示已在用户的打印机上物理出纸。目标 Qring 固件是否接受全部命令仍需实机验收。

## 画布与打印一致性

- `LabelRenderer` 是唯一排版与二值化实现；编辑画布、最终预览和蓝牙发送复用同一个 `RenderedLabel` 位图。
- 屏幕只按纸宽等比显示打印点阵，并使用最近邻采样；不会在 Compose 中再次排版文字或平滑二维码/抖动点。
- 最终打印按钮在预览哈希未对应当前文档时保持禁用，避免把上一帧位图发送给打印机。
- 10.0–57.0 mm 每 0.1 mm、左右装纸均验证纸面坐标和打印头坐标可逆；多余打印头列保持白色且不会越过配置纸边。
- 最终预览在纸张设置改变而新点阵尚未完成时隐藏旧位图，并显示同步状态；靠左/靠右高亮使用与打印裁切相同的 `PaperPlacement`，不再出现旧预览方向与新设置相反的瞬时画面。

## 蓝牙扫描与自动重连

- Android 13–16 的系统蓝牙发现广播按导出接收器注册，补充发现开始、设备名称变化、发现完成和蓝牙开关状态处理。
- 首次授权结果通过 Activity Result 立即回写界面状态；从系统应用权限页返回也会重新检查，授权卡片不再因旧的 Compose 状态滞留。授权后会自动继续刚才的扫描；扫描按钮有进度动画并可停止，20 秒保护超时避免永久停在“扫描中”。
- Qring 名称优先于 BeePrt、BY 和普通设备；已配对设备会先显示，发现结果按地址去重。
- 扫描时暂停自动重连，扫描结束恢复；重连使用 1/2/4/8/15/30 秒有上限退避，并在手动断开、扫描、连接或打印期间停止竞争。
- 批量打印等待窗口会先消费当前所有可用回包字节再判定，同一窗口内 ACK `0xAA` 优先于瞬时故障帧。只有 `FF 01..04` 是合法打印故障帧，并需持续确认才中断；只有明确 `FF 03` 触发过热断点恢复。
- 同一记录的 1–50 份按份发送完整 `清残留回包 → ENABLE → ENABLE_SECONDARY → WAKE_UP → GS v 0 → STOP → ACK` 会话；普通内容复用已编码点阵，含流水号的任务按份重新渲染，标签间隙作为该份点阵尾部空白行发送。
- ACK 只表示本份数据被固件接受，不直接等同纸张已完全走出。每份 ACK 后必须同时满足按点阵高度计算的机械截止时间和固定 6.5 秒固件复位截止时间，取较晚时刻；完成后才增加已完成份数并发送下一份。
- 每次新会话前还需等待输入流连续 300 ms 安静，清除迟到 ACK/故障尾字节，防止上一份的 `0xAA` 被下一份误认。批量期间不发送状态查询，避免标签间隙把红外纸张位、温度位或忙闲位变成假故障；首页空闲状态和手动走纸仍可查询，单次温度快照不会显示过热。
- 上述状态机与排序策略已通过单元测试；真实广播、配对和 SPP 握手仍需目标手机/打印机实测。

## 走纸、设备校准与电量读取

- 连续纸新建文档、JSON 缺省导入和行业模板的尾部走纸默认值共用 `DEFAULT_TAIL_FEED_MM = 5`，避免不同入口出现 5/6/8 mm 分叉。
- 2.5.1 首次启动只迁移旧版恰好为 8 mm 的默认值；0–50 mm 范围内其他手动设置原样保留。
- 标签纸仍使用独立 `labelGapMm`。Qring SPP 不再依赖点阵结束后可能被固件忽略的单独 `ESC J`，而是把间隙换算成真实空白光栅行追加到同一幅点阵高度中；通用 ESC/POS 仍使用尾部走纸命令。
- 横向/纵向校准会在新建、模板、历史和导入文档进入编辑器时统一应用；更换 DPI/打印头点数时按物理毫米缩放元素，不改变任务自己的纸型、宽高、间隙和浓度。设置输入允许负数和小数的中间编辑状态。
- 电量仅接受 Qring 私有查询两字节响应中的第二字节且必须为 0–100；`0xFF`、超范围或短响应显示 `--`，不再夹到 100%。
- 单元测试验证默认 5 mm、标签间隙空白光栅、校准传播/DPI 换算以及电量有效值和无效哨兵值。
- 首页电量/纸张状态下方新增带图标的“走纸”入口；使用和新建打印任务一致的 Material 底部展开动效，支持 0.1–100 mm 手动输入以及 1/2/3/4/5 mm 快捷项。UI 和两种协议共用 `PrintJobPolicy.manualFeedPlan` 进行校验、DPI 毫米换算与命令分段，不复制走纸算法；Qring 分支在发送 `STOP` 前等待电机完成，避免固件把同一蓝牙突发中的独立走纸命令取消。

## 尚不能在本机构建环境证明的内容

1. 小米 Android 16 上 PP-OCRv6 Small 的实际耗时、峰值内存、CameraX 对焦和票据识别率；当前 `adb devices -l` 无连接设备。
2. 用户实际数据集能达到的准确率；必须逐字标注并计算编辑距离，模型置信度不能替代真实准确率。
3. 蓝牙发现、配对、自动重连、设备私有电量/纸张状态与真实打印机固件响应。
4. 10–57 mm 实际纸张左右装入后的机械偏移和毫米误差。

只有完成目标手机与打印机清单后，才能把相应硬件组合标记为“实机通过”。
