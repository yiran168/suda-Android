# 素打本地功能差距审计

更新日期：2026-08-13

本审计区分“已有真实实现”“已有但不完整”“缺少且可本地实现”以及“依赖云端或特定硬件”。只有源码中存在完整数据流并有测试/构建验证的功能才标记为已实现。

## 对照资料

- 微打 App Store 公开说明：<https://apps.apple.com/cn/app/%E5%BE%AE%E6%89%93/id1101579363>
- 汉印 HereLabel 官方页面：<https://hm.hprt.com/en/products/software/app/>
- 汉印“汉码”App Store 公开说明：<https://apps.apple.com/cn/app/%E6%B1%89%E7%A0%81/id1288952997>

## 已有真实实现

- 蓝牙 SPP 连接、设备发现、自动重连、连接状态、兼容 Qring 私有协议与通用 ESC/POS。
- Qring 设备电量、纸张/故障状态查询；不支持该私有指令的机器会明确降级而不伪造状态。
- 连续纸自动算长；标签纸固定宽高；10–57 mm 无级纸宽；小于 55 mm 左/右装纸确认与打印头窗口裁剪。
- 203 dpi 点阵预览与实际打印共用同一渲染结果，支持窄纸偏移与校准。
- 文字、图片、二维码、一维码、形状、表格、日期时间、流水号、手绘等可编辑元素。
- 拖动、缩放、旋转、四边/四角调整、多选、成组、锁定、撤销/重做、吸附与吸附解除。
- 18 个基础字体选项（含 4 个内置中文字体）、经 Android 字体解析器验证的本地 TTF/OTF 导入、私有缓存、缺失回退、100–900 连续字重和 400/700 快捷值。
- 20 种编码类型、编码内容标准化、规则校验与可靠回退。
- 22 种形状、8 种抖动/半色调算法、亮度、对比度、阈值、反色与图片适配。
- 拍照扫描文字、拍照扫描建模板、实时识码、相册识字、相册识码、拍照打印。
- 文字 OCR 先点按对焦并拍摄高分辨率 JPEG，再按可拖动蓝框裁切后执行一次 PP-OCRv6 Small；不再使用低分辨率实时帧 OCR。实时分析只保留二维码/条码。
- 494 个本地模板、12 个行业分类、模板内容拆分为可编辑文字/编码/线条及可替换图形层。
- 本地模板保存、收藏、最近使用、JSON 导入导出、扫码匹配商品/模板、打印历史、异常报告复制/分享/本地 Markdown 保存。
- 本地商品资料库 CRUD 与字段搜索；Excel/WPS/CSV 多表导入；同条码/SKU 更新；商品变量套用或生成可编辑商品标签。
- PDF 和纯文本分页直印；系统分享的图片、PDF、文本、表格和模板可直接进入对应本地工作流。
- Android 系统分享入口：WPS/文件管理器分享 Excel、WPS、CSV、TSV 或素打 JSON 模板；相册分享图片时复制到应用私有目录并直接打开画布。
- 10 套具有独立构图的主题（含 4 套玻璃主题）和 12 种音效、随机/算法生成音效。

## 变量数据与批量打印现状

### 已实现

- RFC 风格 CSV 引号、换行、重复字段名处理。
- CSV、TSV、分号分隔文本、UTF-8/UTF-16/GB18030。
- `.xlsx` 离线读取，共享字符串、内联字符串、数字和布尔值。
- Excel/WPS 常用日期、时间、百分比、千分位、小数位和科学计数格式按单元格样式还原。
- 旧式 Excel `.xls`、兼容 BIFF 的 WPS `.et`，以及 WPS 另存为 `.xlsx`、CSV、TSV 后导入。
- 首个工作表为空时会自动寻找后续包含字段名和记录的工作表。
- 多工作表文件会列出所有有效工作表，可按真实工作表名称手动切换；切换后预览与批打范围同步重置。
- `{{字段名}}` 绑定到文字、条码、表格、流水号等打印内容。
- 当前记录实时预览；打印时逐行替换、逐张渲染并顺序发送。
- 每条记录 1–50 份，打印结果写入本地历史。
- 可选择批打起止行；显示记录/份数进度；可在当前标签完成后安全停止，并从精确位置继续。
- 可跨字段筛选，并按任一字段做稳定的数值/文本升降序排列；画布预览、范围选择和最终打印严格使用同一行序。
- 可选择失败时停在精确份数续打，或记录完整失败清单并跳过当前记录继续下一条。

### 格式边界

- WPS 专有且非 BIFF/OOXML 的 `.et` 仍需在 WPS 中另存为 `.xlsx/CSV`。
- Excel 极少见的条件格式、区域性会计格式或公式但没有缓存结果时，显示值仍可能与桌面 Excel 不同；导入后应在变量预览中确认。

## 已完成的原差距清单

上一版列出的 Excel 显示格式、PDF/文本直印、本地字体、扫码模板匹配、本地商品资料、收藏与最近使用均已实现并纳入单元测试/构建验证。当前审计没有再发现可用一个通用离线实现且不依赖厂商私有协议的同级缺口。

## 不纳入“纯本地通用功能”

- 云模板目录、团队协作、账号同步、远程云打印。
- NFC 碰一碰、RFID 耗材识别、Wi‑Fi/局域网专有打印协议：需要目标打印机硬件及厂商协议，不能在没有协议与设备验证的情况下伪造可用性。
- SQL/Access 云端或局域网数据库连接：涉及外部服务、凭据和网络安全配置，应作为独立可选模块，而不是默认离线功能。

## 验收原则

- 不以入口、按钮或静态演示代替功能实现。
- 所有导入数据先做大小、行列数、编码和格式校验。
- 批量任务必须逐条记录进度，失败不隐瞒，不重复打印已确认完成的份数。
- 相机/OCR 的“稳定度”不冒充真实准确率；准确率必须使用带标准答案的数据集统计。
- OCR 结果可逐行校对；只有用户明确确认的标准答案才计入累计字符准确率，本机纠错规则需相同错误重复确认两次才自动应用。
- 每个新增模块均需单元测试、Lint 和 Android 7–16 兼容构建验证；摄像头、蓝牙与出纸结果必须另在目标手机和打印机上完成实机验收，不得用编译成功代替实机通过。
